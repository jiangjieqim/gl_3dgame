#version 110

uniform sampler2D texture1;

void main(void){
	vec4 finalColor=texture2D(texture1, gl_TexCoord[0].st);

	vec4 color = vec4(1,0,0,1);//这里可以设置字体的颜色

/*
	if(finalColor.a<=0.0){
		discard;
	}else{
		gl_FragColor = finalColor;//取字库中的像素颜色
	}
*/

	if(finalColor.r + finalColor.g + finalColor.b == 0.0f)	{
		discard;//黑色的扣去
	}else{
		gl_FragColor = finalColor;
	}

//	gl_FragColor = finalColor;
}